# Copy to deploy/attend.env on the VPS (do not commit secrets).
# ATTEND_API_HOST must match the DNS A record (no https:// prefix).
ATTEND_API_HOST=api.builditmuj.club
CERTBOT_EMAIL=admin@builditmuj.club
# Include the student portal origin so browsers can call the API.
# CORS_ORIGINS=https://register.builditmuj.club
