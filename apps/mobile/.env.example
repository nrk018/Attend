# Optional override for a physical device (same WiFi as the Mac).
# Simulator always uses http://localhost:8000 and ignores this.
# Uncomment and set to: ipconfig getifaddr en0
# EXPO_PUBLIC_API_URL=http://10.37.128.17:8000
