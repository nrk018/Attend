# Optional override — normally auto-detected from Expo/Metro host on physical device (same WiFi).
# Uncomment only if auto-detection fails:
# EXPO_PUBLIC_API_URL=http://10.183.111.17:8000
#
# Find IP: ipconfig getifaddr en0
